[project]
name = "mneme-ingest"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.0",
    "qdrant-client>=1.10.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
