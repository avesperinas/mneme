---
tags:
  - core
---

# Retrieval

Covers search. Links back to [[Architecture]].

## Dense Search

Uses embeddings.

```python
# this #fake-tag and [[FakeLink]] live inside code and must be ignored
print("hello")
```

Inline `#alsofake` and `[[AlsoFake]]` are ignored too.
